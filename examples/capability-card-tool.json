{
  "capability_id": "cap_read_repo",
  "kind": "tool",
  "name": "read_repo",
  "description": "Read repository file content",
  "input_schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string"
      }
    },
    "required": [
      "path"
    ]
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "content": {
        "type": "string"
      }
    },
    "required": [
      "content"
    ]
  },
  "risk_class": "R1"
}