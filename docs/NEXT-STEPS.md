# OAPS Next Steps

This file is the short operational queue for the next long-running Codex tranche.

## Current Priority Order

1. Decide whether to formalize event replay semantics further in the HTTP binding draft.
2. Add a cloud-task variant or SDK supervisor variant if local loop usage proves insufficient.

## Usage Rule

If `PLANS.md` contains many unfinished tranches, use this file as the short-horizon priority override.
