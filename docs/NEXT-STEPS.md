# OAPS Next Steps

This file is the short operational queue for the next long-running Codex tranche.

## Current Priority Order

1. Add runtime-backed conformance scenarios where the current reference implementation already supports them.
2. Tighten profile mapping notes without overclaiming unsupported behavior.
3. Exercise the local Codex harness on a real unattended multi-tranche run and capture findings in `docs/STATUS.md`.
4. Add a clearer “what is stable vs draft vs concept” matrix to top-level docs.

## Usage Rule

If `PLANS.md` contains many unfinished tranches, use this file as the short-horizon priority override.
