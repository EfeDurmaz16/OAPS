# OAPS Next Steps

This file is the short operational queue for the next long-running Codex tranche.

## Current Priority Order

1. Expand compatibility declaration surfaces:
   - example result payloads
   - compatibility statement docs
   - scope-level summary output from Python
2. Add runtime-backed conformance scenarios where the current reference implementation already supports them.
3. Tighten profile mapping notes without overclaiming unsupported behavior.
4. Exercise the local Codex harness on a real unattended multi-tranche run and capture findings in `docs/STATUS.md`.

## Usage Rule

If `PLANS.md` contains many unfinished tranches, use this file as the short-horizon priority override.
